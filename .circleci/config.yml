version: 2.1

orbs:
  welcome: circleci/welcome-orb@0.4.1

jobs:
  build:
    working_directory: /project
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.1
    steps:
      - checkout
      - run: dotnet build backend/RiskManagementAPI.sln


workflows:
  build_and_test:
    jobs:
      - build
  welcome:
    jobs:
      - welcome/run
